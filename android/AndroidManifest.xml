<?xml version="1.0" encoding="utf-8"?>
<!-- Envoltorio nativo: una sola Activity con un WebView a pantalla completa
     que carga el juego desde assets/www. Sin permisos: todo va dentro del APK. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rocanegra.td"
    android:versionCode="3"
    android:versionName="2.1">

    <!-- Android 15+ rechaza instalar apps que apunten por debajo de API 24, así
         que el objetivo va alto aunque se compile contra android.jar de API 23:
         la Activity no usa nada por encima de ese nivel. -->
    <uses-sdk android:minSdkVersion="23" android:targetSdkVersion="34" />

    <!-- El juego necesita WebGL 2 (GLES 3), pero se declara opcional para que
         ningún instalador rechace el APK por la comprobación de la característica. -->
    <uses-feature android:glEsVersion="0x00030000" android:required="false" />
    <!-- El horizontal se pide, no se exige: que no descarte a ningún dispositivo. -->
    <uses-feature android:name="android.hardware.screen.landscape" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|uiMode"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
